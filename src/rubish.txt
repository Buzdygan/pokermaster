
/* tested hand scores in the evaluator */
void testEvaluator()
{
    HandEvaluator *evaluator = new HandEvaluator();
    while(true)
    {
        std::vector<int> cards;
        printf("czytam\n");
        for (int i = 0; i < 7; i ++)
        {
            int a;
            scanf("%d", &a);
            if (a == -1)
                return;
            cards.push_back(a);
        }
        printHand(cards);
        printf(": %d\n", evaluator -> evaluateHand(cards));
    }
}
